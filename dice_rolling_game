import random
 while True:
    choice = input("role a dice (y/n) ?).lower()
    if choice == "y":
      die1 = random.randint(1,6)
      die2 = random.randint(i,6)
      print(f'({die1},{die2})')
    elif choice == "n:
      print("Thank you for playing game!")
    break
    else:
      print("Invalid Choice!")
